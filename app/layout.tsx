import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Indian Coffee Guide - Discover Great Indian Coffee",
  description: "Explore and discover the finest Indian coffee and roasters. Your guide to finding the perfect cup of Indian coffee.",
  keywords: "Indian coffee, coffee roasters, specialty coffee, coffee guide, coffee marketplace",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  );
}
